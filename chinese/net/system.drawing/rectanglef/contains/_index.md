---
title: Contains
second_title: Aspose.Drawing for .NET API 参考
description: 确定指定点是否包含在此范围内RectangleF结构.
type: docs
weight: 180
url: /zh/net/system.drawing/rectanglef/contains/
---
## Contains(float, float) {#contains}

确定指定点是否包含在此范围内RectangleF结构.

```csharp
public bool Contains(float x, float y)
```

| 范围 | 类型 | 描述 |
| --- | --- | --- |
| x | Single | 他要测试的点的 x 坐标。 |
| y | Single | 要测试的点的 y 坐标。 |

### 返回值

这个方法返回`真的`如果 x 和 y 定义的点包含在 this 中RectangleF结构体;否则`错误的`.

### 也可以看看

* struct [RectangleF](../../rectanglef)
* 命名空间 [System.Drawing](../../rectanglef)
* 部件 [Aspose.Drawing](../../../)

---

## Contains(PointF) {#contains_1}

确定指定点是否包含在此范围内RectangleF结构.

```csharp
public bool Contains(PointF pt)
```

| 范围 | 类型 | 描述 |
| --- | --- | --- |
| pt | PointF | 这PointF去测试。 |

### 返回值

这个方法返回`真的`如果 pt 参数表示的点包含在 this 中RectangleF结构体;否则`错误的`.

### 也可以看看

* struct [PointF](../../pointf)
* struct [RectangleF](../../rectanglef)
* 命名空间 [System.Drawing](../../rectanglef)
* 部件 [Aspose.Drawing](../../../)

---

## Contains(RectangleF) {#contains_2}

确定矩形区域是否由*rect*完全包含在这个RectangleF结构.

```csharp
public bool Contains(RectangleF rect)
```

| 范围 | 类型 | 描述 |
| --- | --- | --- |
| rect | RectangleF | 这RectangleF去测试。 |

### 返回值

这个方法返回`真的`如果矩形区域由*rect*完全包含在由此表示的矩形区域内RectangleF;否则`错误的`.

### 也可以看看

* struct [RectangleF](../../rectanglef)
* 命名空间 [System.Drawing](../../rectanglef)
* 部件 [Aspose.Drawing](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Drawing.dll -->
