---
title: EncoderValue
second_title: Referencia de Aspose.Drawing para .NET API
description: Se usa para especificar el valor del parámetro pasado a un codificador de imágenes JPEG o TIFF cuando se usa elEncoderParameters oEncoderParameters métodos.
type: docs
weight: 720
url: /es/net/system.drawing.imaging/encodervalue/
---
## EncoderValue enumeration

Se usa para especificar el valor del parámetro pasado a un codificador de imágenes JPEG o TIFF cuando se usa elEncoderParameters) oEncoderParameters) métodos.

```csharp
public enum EncoderValue
```

### Valores

| Nombre | Valor | Descripción |
| --- | --- | --- |
| ColorTypeCMYK | `0` | No se usa en GDI+ versión 1.0. |
| ColorTypeYCCK | `1` | No se usa en GDI+ versión 1.0. |
| CompressionLZW | `2` | Especifica el esquema de compresión LZW. Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría Compresión. |
| CompressionCCITT3 | `3` | Especifica el esquema de compresión CCITT3. Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría de compresión. |
| CompressionCCITT4 | `4` | Especifica el esquema de compresión CCITT4. Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría de compresión. |
| CompressionRle | `5` | Especifica el esquema de compresión RLE. Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría de compresión. |
| CompressionNone | `6` | No especifica compresión. Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría de compresión. |
| ScanMethodInterlaced | `7` | No se usa en GDI+ versión 1.0. |
| ScanMethodNonInterlaced | `8` | No se usa en GDI+ versión 1.0. |
| VersionGif87 | `9` | No se usa en GDI+ versión 1.0. |
| VersionGif89 | `10` | No se usa en GDI+ versión 1.0. |
| RenderProgressive | `11` | No se usa en GDI+ versión 1.0. |
| RenderNonProgressive | `12` | No se usa en GDI+ versión 1.0. |
| TransformRotate90 | `13` | Especifica que la imagen debe girarse 90 grados en el sentido de las agujas del reloj alrededor de su centro. Se puede pasar al codificador JPEG como un parámetro que pertenece a la categoría de transformación. |
| TransformRotate180 | `14` | Especifica que la imagen debe girarse 180 grados sobre su centro. Se puede pasar al codificador JPEG como un parámetro que pertenece a la categoría de transformación. |
| TransformRotate270 | `15` | Especifica que la imagen debe rotarse 270 grados en el sentido de las agujas del reloj alrededor de su centro. Se puede pasar al codificador JPEG como un parámetro que pertenece a la categoría de transformación. |
| TransformFlipHorizontal | `16` | Especifica que la imagen se volteará horizontalmente (sobre el eje vertical). Se puede pasar al codificador JPEG como un parámetro que pertenece a la categoría de transformación. |
| TransformFlipVertical | `17` | Especifica que la imagen se volteará verticalmente (sobre el eje horizontal). Se puede pasar al codificador JPEG como un parámetro que pertenece a la categoría de transformación. |
| MultiFrame | `18` | Especifica que la imagen tiene más de un marco (página). Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría de marca de guardado. |
| LastFrame | `19` | Especifica el último fotograma de una imagen de varios fotogramas. Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría de marca de guardado. |
| Flush | `20` | Especifica que se debe cerrar un archivo o secuencia de varios fotogramas. Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría de marca de guardado. |
| FrameDimensionTime | `21` | No se usa en GDI+ versión 1.0. |
| FrameDimensionResolution | `22` | No se usa en GDI+ versión 1.0. |
| FrameDimensionPage | `23` | Especifica que se agregará un marco a la dimensión de página de una imagen. Se puede pasar al codificador TIFF como un parámetro que pertenece a la categoría de marca de guardado. |

### Ver también

* espacio de nombres [System.Drawing.Imaging](../../system.drawing.imaging)
* asamblea [Aspose.Drawing](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Drawing.dll -->
